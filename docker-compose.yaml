version: '3.8'

services:
  # web_entrypoint:
  #   build: ./web_entrypoint/
  #   ports:
  #     - 5001:5000
  #   volumes:
  #     - ./web_entrypoint/src/build/web:/app
  
    # cache_app:
    #   build: ./cache_app/
    #   volumes:
    #     - ./cache_app/src:/app/src
    #     - ./common:/app/common
    #   depends_on:
    #     - rabbit
    #     - redis
    #   env_file:
    #     - config.env
  
    # redis:
    #   image: "redis:alpine"
    #   command: redis-server
    #   volumes:
    #     - ./redis/redis_data:/var/lib/redis
    #   env_file:
    #     - config.env
    
  # rabbit:
  #   image: rabbitmq:3.7-management
  #   volumes:
  #   - ./rabbitmq/etc/:/etc/rabbitmq/
  #   - ./rabbitmq/data/:/var/lib/rabbitmq/
  #   - ./rabbitmq/logs/:/var/log/rabbitmq/
  #   env_file:
  #     - config.env

  server:
    build: ./server/
    volumes:
      - ./server/app:/app/app
      - ./common:/app/common
    ports:
      - 5002:5000
    depends_on:
      # - rabbit
      - server_db
    env_file:
      - config.env
  
  server_db:
    image: postgres
    ports:  # for debug purpuses
      - 5003:5432
    volumes:
      - ./server_db/postgres_data:/var/lib/postgresql/data/
      - ./server_db/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d/
    env_file:
      - config.env    